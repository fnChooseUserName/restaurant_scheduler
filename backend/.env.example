# Local dev (Postgres on host). For Docker Compose, use host `db` (see README).
DATABASE_URL=postgresql://postgres:password@localhost:5432/restaurant_scheduler
# DATABASE_URL=postgresql://postgres:password@db:5432/restaurant_scheduler
PORT=3000
NODE_ENV=development
